{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "npm ci --include=dev && npm run build --workspace=@treasuryos/types && npm run build --workspace=@treasuryos/reporter",
    "buildEnv": {
      "BP_DISABLE_RUST": "true",
      "BP_DISABLE_RUST_TOOLCHAIN": "true",
      "NODE_ENV": "production"
    }
  },
  "deploy": {
    "startCommand": "npm run start:prod --workspace=@treasuryos/reporter",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10,
    "healthcheckPath": "/api/health",
    "healthcheckTimeout": 60,
    "environment": "production"
  }
}
